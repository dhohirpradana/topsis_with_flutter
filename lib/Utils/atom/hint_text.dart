hintText(index) => (index == 0)
    ? '1 - 30 cm'
    : (index == 1)
        ? '1 - 4 poin'
        : (index == 2)
            ? '1 - 4 poin'
            : (index == 3)
                ? '1 - 4 poin'
                : '1 - 4 poin';

hintTextKriteria(index) => (index == 0)
    ? 'Bobot 1 - 5'
    : (index == 1)
        ? 'Bobot 1 - 5'
        : (index == 2)
            ? 'Bobot 1 - 5'
            : (index == 3)
                ? 'Bobot 1 - 5'
                : 'Bobot 1 - 5';
