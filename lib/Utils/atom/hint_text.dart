hintText(index) => (index == 0)
    ? '1 - 50 cm'
    : (index == 1)
        ? '1 - 10 poin'
        : (index == 2)
            ? '1 - 10 poin'
            : (index == 3)
                ? '1 - 10 poin'
                : '1 - 10 poin';

hintTextKriteria(index) => (index == 0)
    ? 'Bobot 1 - 5'
    : (index == 1)
        ? 'Bobot 1 - 5'
        : (index == 2)
            ? 'Bobot 1 - 5'
            : (index == 3)
                ? 'Bobot 1 - 5'
                : 'Bobot 1 - 5';
