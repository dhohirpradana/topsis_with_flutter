hintText(index) => (index == 0)
    ? '1 - 3 meter'
    : (index == 1)
        ? '1 - 3'
        : (index == 2)
            ? '1 - 3'
            : (index == 3)
                ? '1 - 3'
                : '1 - 3';

hintTextKriteria(index) => (index == 0)
    ? 'Bobot 1 - 5'
    : (index == 1)
        ? 'Bobot 1 - 5'
        : (index == 2)
            ? 'Bobot 1 - 5'
            : (index == 3)
                ? 'Bobot 1 - 5'
                : 'Bobot 1 - 5';
